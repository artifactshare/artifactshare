// Retained only so older tooling can name the former variable while proving
// that it no longer grants access. Activity-lock authorization is process
// local and cannot be supplied through the environment.
export const ACTIVITY_LOCK_HELD_ENV = 'ARTIFACTSHARE_ACTIVITY_LOCK_HELD'
