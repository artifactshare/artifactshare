# Upload site sample

This folder is a flat static site for manual upload testing.

Select these files together in the upload dialog:

- `index.html`
- `site.css`
- `site.js`
- `checks.json`

The current upload flow stores each file by its file name, so this sample avoids
subfolders. After upload, the page should show CSS, JavaScript, and JSON as
loaded.
